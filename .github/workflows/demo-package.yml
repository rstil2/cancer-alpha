# Disable legacy Streamlit ZIP — native app workflow handles releases.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use demo-native.yml for releases"
